/**
 * Premium scaffolding (docs/REVAMP_SPEC.md §5). Flip to `true` once
 * billing exists; until then `usePremium().can()` always returns true
 * and nothing in the app is actually gated.
 */
export const PAYWALL_ENFORCED = false;
