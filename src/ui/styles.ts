/**
 * Shared type-scale class strings (docs/REVAMP_SPEC.md §1). Use these
 * instead of re-typing the utility soup so the display/caption styles
 * stay identical everywhere.
 */

/** Page title — Sora 26/32 700. */
export const H1 = 'font-display text-[26px] leading-8 font-bold tracking-[-0.02em] text-text';
/** Card title — Sora 18/24 700. */
export const H2 = 'font-display text-lg leading-6 font-bold tracking-[-0.01em] text-text';
/** Stat numeral — Sora 24/28 700 tabular. */
export const STAT = 'font-display text-2xl leading-7 font-bold tracking-[-0.02em] tabular-nums';
/** Caption — 11/16 700 uppercase, .08em tracking, subtle. */
export const CAPTION = 'text-[11px] leading-4 font-bold uppercase tracking-[.08em] text-subtle';
/** Secondary line under a title — 13px muted. */
export const SUB = 'text-[13px] leading-[18px] text-muted';
