# Copy to .env.local (or set in CI) to enable features.

# Web Push — paste the public VAPID key from
# Firebase console → Project settings → Cloud Messaging → Web Push certificates.
# Without this, Settings → Push notifications stays disabled (soft-fail).
VITE_FCM_VAPID_KEY=
